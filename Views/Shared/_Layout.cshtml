<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SemesterTwo</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/SemesterTwo.styles.css" asp-append-version="true" />
    <style>
        /* General Styling */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f9f5f3; /* Beige background */
            color: #333;
        }

        /* Navbar Styling */
        .navbar {
            background-color: #fce4ec; /* Light pink */
            border-bottom: 2px solid #f8bbd0; /* Slightly darker pink */
        }

        .navbar-brand {
            color: #880e4f; /* Dark pink */
        }

        .nav-link {
            color: #ad1457; /* Medium pink */
        }

        .nav-link:hover {
            color: #880e4f; /* Dark pink on hover */
        }

        /* Footer Styling */
        footer {
            background-color: #f8bbd0; /* Light pink */
            color: #fff;
            padding: 20px 0;
        }

        footer a {
            color: #ffffff; /* White text */
        }

        /* Main Content Styling */
        main {
            padding: 20px;
            background-color: #ffffff; /* White background */
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Button Styling */
        button {
            background-color: #e91e63; /* Vivid pink */
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            margin-top: 10px;
        }

        button:hover {
            background-color: #c2185b; /* Darker pink on hover */
        }

        /* Form Styling */
        input[type="text"],
        input[type="email"],
        input[type="file"] {
            border: 1px solid #f8bbd0; /* Light pink border */
            border-radius: 4px;
            padding: 8px;
            margin: 8px 0;
            width: 100%;
        }

        input[type="text"]:focus,
        input[type="email"]:focus,
        input[type="file"]:focus {
            border-color: #880e4f; /* Dark pink on focus */
            outline: none;
        }

      